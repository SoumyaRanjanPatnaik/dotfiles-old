#!/usr/bin/bash

mkdir $1
cd $1
cp ~/Templates/competitive/template.cpp main.cpp
cp ~/Templates/competitive/Makefile ./
touch input.txt
alacritty msg create-window -e bash -c "nvim $PWD/input.txt && fish" 
alacritty msg create-window -e bash -c "cd $PWD && fish"
nvim main.cpp
